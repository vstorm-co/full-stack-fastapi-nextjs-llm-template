name: Deploy Documentation

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Configure Git Credentials
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.14'

      - name: Install uv
        uses: astral-sh/setup-uv@v10.1.0

      - name: Install dependencies
        run: uv sync --extra docs

      - name: Deploy to GitHub Pages
        # --no-history replaces gh-pages with a single fresh commit each run.
        # Avoids the "remote: fatal error in commit_refs" rejection we hit
        # when the branch accumulates churn / hits a server-side limit.
        run: uv run mkdocs gh-deploy --force --no-history
